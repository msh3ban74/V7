name: Build APK from ZIP
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build and Unzip
        run: |
          # العثور على أحدث ملف ZIP وفك ضغطه فقط
          LATEST_ZIP=$(ls -t *.zip | head -n 1)
          echo "Building from: $LATEST_ZIP"
          unzip -q "$LATEST_ZIP" -d project
          
          # الانتقال لمجلد المشروع
          cd "$(find project -name "gradlew" -exec dirname {} \; | head -n 1)"
          
          # تنظيف الـ Wrapper لضمان سلامة الملفات
          rm -rf gradle/wrapper/gradle-wrapper.jar
          ./gradlew wrapper --gradle-version 8.5
          
          # بناء التطبيق
          sh gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: '**/build/outputs/apk/debug/app-debug.apk'
